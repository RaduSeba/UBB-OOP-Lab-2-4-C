#include "Sort.h"


void sort(DynamicArray* l, FunctieComparare cmpF)
{
	int i, j;
	for (i = 0; i < getLengt(l); i++)
	{
		for (j = i + 1; j < getLengt(l); j++)
		{
			Tranzactie p1= getTranzOnPoz(l,i);
		}
	}
}